package constants

// GitDir is directory under which all the housekeeping of repository happens:
// all the objects are stored as well as references to them
const GitDir = ".gitik"

// HeadName is filename that should contain object id of the most recent commit
const HeadName = "HEAD"
